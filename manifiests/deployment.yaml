apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: carlosfonsecam/nginx-openshift:v4
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: tmp 
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}